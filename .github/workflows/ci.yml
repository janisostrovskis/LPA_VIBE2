name: CI

on:
  push:
    branches:
      - master
      - main
  pull_request:
    branches:
      - master
      - main

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install backend dev dependencies
        run: cd backend && pip install -e .[dev]

      - name: Ruff (Python)
        run: cd backend && ruff check .

      - name: ESLint (TypeScript)
        if: hashFiles('frontend/package.json') != ''
        run: cd frontend && npm ci && npx eslint .

  typecheck:
    name: Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install backend dev dependencies
        run: cd backend && pip install -e .[dev]

      - name: mypy (Python)
        # R3 mitigation: skip when backend/app does not exist yet to avoid
        # "no modules to check" error on the empty repo.
        if: hashFiles('backend/app/**/*.py') != ''
        run: cd backend && mypy app

      - name: tsc (TypeScript)
        if: hashFiles('frontend/package.json') != ''
        run: cd frontend && npm ci && npx tsc --noEmit

  cola-check:
    name: COLA Import Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Check COLA import rules
        run: python scripts/check_cola_imports.py

  filesize-check:
    name: File Size Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Check file sizes
        run: python scripts/check_file_size.py

  unit-tests:
    name: Unit Tests
    runs-on: ubuntu-latest
    needs: [lint, typecheck]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install backend dev dependencies
        run: cd backend && pip install -e .[dev]

      - name: pytest unit tests
        if: hashFiles('backend/tests/unit/**/*.py') != ''
        run: cd backend && pytest tests/unit -q

      - name: Vitest (frontend unit tests)
        if: hashFiles('frontend/package.json') != '' && hashFiles('frontend/src/**/*.test.*') != ''
        run: cd frontend && npm ci && npx vitest run

  integration-tests:
    name: Integration Tests
    runs-on: ubuntu-latest
    needs: [lint, typecheck]
    # R4 mitigation: skip the whole job when no integration tests exist to
    # avoid spinning up the PostgreSQL service container wastefully.
    if: hashFiles('backend/tests/integration/**/*.py') != ''
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: ci
          POSTGRES_DB: lpa_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install backend dev dependencies
        run: cd backend && pip install -e .[dev]

      - name: pytest integration tests
        run: cd backend && pytest tests/integration -q
        env:
          DATABASE_URL: postgresql+psycopg://postgres:ci@localhost:5432/lpa_test

  e2e:
    name: E2E Tests
    runs-on: ubuntu-latest
    if: (github.ref == 'refs/heads/master' || github.ref == 'refs/heads/main') && hashFiles('frontend/playwright.config.ts') != ''
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install backend dev dependencies
        run: cd backend && pip install -e .[dev]

      - name: Install frontend dependencies
        run: cd frontend && npm ci

      - name: Install Playwright browsers
        run: cd frontend && npx playwright install --with-deps

      - name: Run Playwright E2E tests
        run: cd frontend && npx playwright test

  security:
    name: Security Scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install backend dev dependencies
        run: cd backend && pip install -e .[dev]

      - name: bandit (Python SAST)
        # -ll = medium+ severity only; exits 1 on any finding.
        if: hashFiles('backend/app/**/*.py') != ''
        run: bandit -r backend/app -ll

      - name: semgrep
        # R6: pinned configs, never --config auto (non-reproducible).
        run: >
          semgrep scan
          --config p/python
          --config p/security-audit
          --error
          backend/

      - name: pip-audit
        run: cd backend && pip-audit --strict

      - name: Download gitleaks
        run: |
          curl -sSL \
            https://github.com/gitleaks/gitleaks/releases/download/v8.21.0/gitleaks_8.21.0_linux_x64.tar.gz \
            -o /tmp/gitleaks.tar.gz
          tar -xzf /tmp/gitleaks.tar.gz -C /tmp gitleaks
          chmod +x /tmp/gitleaks
          /tmp/gitleaks version

      - name: gitleaks (staged files)
        run: >
          /tmp/gitleaks detect
          --source .
          --no-banner
          --redact
          --exit-code 1

      - name: gitleaks (full history)
        run: >
          /tmp/gitleaks detect
          --source .
          --no-banner
          --redact
          --exit-code 1
          --log-opts="--all"

      - name: scripts/security_scan.py
        run: python scripts/security_scan.py

      - name: npm audit
        if: hashFiles('frontend/package.json') != ''
        run: cd frontend && npm audit --audit-level=moderate
